---
import Base from "./Base.astro";

interface Props {
  title: string | undefined;
}

const { title } = Astro.props;
---

<Base {title}>
  <div mx-auto max-w="[76ch]">
    <slot />
  </div>
</Base>
